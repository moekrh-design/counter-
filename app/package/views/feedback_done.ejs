<!doctype html>
<%
  const TT_FBD = {
    ar:{title:"تم استلام تقييمك", dir:"rtl", thanks:"شكرًا لك", msg:"تم إرسال تقييمك بنجاح", hint:"سيتم الرجوع تلقائيًا", back:"رجوع", sub:"نشكرك على تقييمك"},
    en:{title:"Feedback received", dir:"ltr", thanks:"Thank you", msg:"Your feedback has been submitted", hint:"Returning automatically", back:"Back", sub:"Thanks for your feedback"},
    fr:{title:"Avis reçu", dir:"ltr", thanks:"Merci", msg:"Votre avis a été envoyé", hint:"Retour automatique", back:"Retour", sub:"Merci pour votre avis"},
    zh:{title:"已收到评价", dir:"ltr", thanks:"谢谢", msg:"您的评价已提交", hint:"即将自动返回", back:"返回", sub:"感谢您的评价"}
  };
  const L = (typeof lang !== 'undefined' && lang) ? lang : 'ar';
  const tr = TT_FBD[L] || TT_FBD.ar;
%>
<html lang="<%= L %>" dir="<%= tr.dir %>">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= tr.title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/public/app.css" rel="stylesheet">
  <link href="/public/moe-ui.css" rel="stylesheet">
</head>

<body class="kiosk-body" data-theme="<%= (typeof ui !== 'undefined' && ui && ui.theme) ? ui.theme : 'dark' %>">

  <div class="kiosk-shell">
    <div class="kiosk-brand-header">
      <div class="kiosk-brand-left"></div>
      <div class="kiosk-brand-center">
        <div class="kiosk-brand-title"><%= (branding && (branding['org_name_'+L] || branding.org_name_ar || branding.org_name_en)) ? (branding['org_name_'+L] || branding.org_name_ar || branding.org_name_en) : (L==='ar'?'وزارة التعليم':'Ministry of Education') %></div>
        <div class="kiosk-brand-sub"><%= (branding && (branding['kiosk_subtitle_'+L] || branding.kiosk_subtitle_ar || branding.kiosk_subtitle_en)) ? (branding['kiosk_subtitle_'+L] || branding.kiosk_subtitle_ar || branding.kiosk_subtitle_en) : tr.sub %></div>
      </div>
      <div class="kiosk-brand-right">
        <% if (branding && branding.logo_url){ %>
          <img class="kiosk-brand-logo" src="<%= branding.logo_url %>" alt="logo">
        <% } %>
      </div>
    </div>

    <div class="kiosk-stage">
      <div class="kiosk-card kiosk-card-hero" style="max-width: 920px;">
        <div class="text-center">
          <% if (ok){ %>
            <div class="display-6 fw-bold mb-2"><%= tr.msg %></div>
            <div class="muted" style="font-size: 20px;">🌿 <%= tr.thanks %></div>
            <div class="muted mt-3"><%= tr.hint %></div>
          <% } else { %>
            <div class="display-6 fw-bold mb-2"><%= (L==='ar') ? 'تعذر استلام التقييم' : (L==='fr' ? 'Impossible d\'envoyer l\'avis' : (L==='zh' ? '无法提交评价' : 'Could not submit feedback')) %></div>
            <div class="muted" style="font-size: 20px;"><%= (L==='ar') ? 'يرجى المحاولة مرة أخرى' : (L==='fr' ? 'Veuillez réessayer' : (L==='zh' ? '请重试' : 'Please try again')) %></div>
          <% } %>
        </div>
      </div>
    </div>
  </div>

  <script>
    setTimeout(()=>{ location.href = '/feedback'; }, 2500);
  </script>

</body>
</html>
